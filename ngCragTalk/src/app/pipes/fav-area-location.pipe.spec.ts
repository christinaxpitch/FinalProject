import { FavAreaLocationPipe } from './fav-area-location.pipe';

describe('FavAreaLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new FavAreaLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
